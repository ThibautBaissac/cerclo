<div class="rounded-xl shadow-sm border border-gray-50 overflow-hidden">
    <div class="p-6 sm:p-8">
    <h2 class="text-xl font-bold text-gray-900 mb-4 flex items-center mt-0">
    <span class="text-indigo-600"><%= icon "user-group", variant: :outline %></span>
    Membres du groupe
    </h2>
    <p class="text-gray-600 mb-4">Les participants déjà membre de ce groupe :</p>
    <div class="flex flex-wrap gap-2">
        <% group.members.each do |member| %>
        <div class="inline-flex items-center px-3 py-1 rounded-full text-sm font-medium bg-gray-100 text-gray-800">
        <%= icon "user", variant: :outline %>
            <%= member.username %>
        </div>
        <% end %>
    </div>
    </div>
</div>
