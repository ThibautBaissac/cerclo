<div class="rounded-xl shadow-sm border border-gray-50 overflow-hidden mb-8">
    <div class="p-6 sm:p-8">
    <h2 class="text-xl font-bold text-gray-900 mb-4 flex items-center mt-0">
        <span class="text-indigo-600"><%= icon "academic-cap", variant: :outline %></span>
        Votre facilitateur
    </h2>
    <div class="flex items-center mb-4">
        <div class="w-12 h-12 bg-indigo-100 rounded-full flex items-center justify-center text-indigo-600 font-bold text-xl mr-3">
        <%= group.facilitator.fullname.split.map(&:first).join[0..1].upcase %>
        </div>
        <div>
        <p class="font-semibold text-gray-900"><%= group.facilitator.fullname %></p>
        <p class="text-sm text-gray-600"><%= group.facilitator.job %></p>
        </div>
    </div>
    <div class="text-gray-600 text-sm">
        <%= group.facilitator.bio %>
    </div>
    </div>
</div>
